{
  "name": "connectting-dl",
  "version": "0.1.4",
  "description": "Lightweight Feishu to Codex CLI bridge for small team deployments.",
  "type": "module",
  "bin": {
    "connectting-dl": "bin/connectting-dl.js"
  },
  "files": [
    "bin",
    "src",
    "config.example.json",
    "README.md",
    "docs"
  ],
  "scripts": {
    "start": "node ./src/cli.js serve",
    "doctor": "node ./src/cli.js doctor",
    "check": "node --check ./src/cli.js && node --check ./src/server.js && node --check ./src/feishu-client.js && node --check ./src/codex-runner.js && node --check ./src/session-store.js && node --check ./src/config.js && node --check ./src/reply-package.js",
    "prepack": "npm run check"
  },
  "keywords": [
    "codex",
    "feishu",
    "lark",
    "bridge",
    "daemon"
  ],
  "author": "DongLi",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  }
}
